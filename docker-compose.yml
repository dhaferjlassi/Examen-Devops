version: "3.7"

services:

  app:
    working_dir: /spring-app
    build:
      context: .
      dockerfile: Dockerfile
    image:  dhafer01/gestion
    restart: on-failure
    container_name: gestion
    depends_on:
      - dbSpring
    networks:
      - spring-network
    ports:
      - 8087:8087
    environment:
      SPRING_APPLICATION_JSON: '{
            "spring.datasource.url"  : "jdbc:mysql://dbSpring:3308/dbgestion?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&useSSL=false&createDatabaseIfNotExists=true",
            "spring.datasource.username" : "root",
            "spring.datasource.password" : "root",
            "spring.jpa.properties.hibernate.dialect" : "org.hibernate.dialect.MySQL5InnoDBDialect",
            "spring.jpa.hibernate.ddl-auto" : "update"
          }'
    volumes:
      - .m2:/root/maven-repo
    stdin_open: true
    tty: true


  dbSpring:
    image: mysql:5.7
    restart: unless-stopped
    container_name: dbSpring
    networks:
      - spring-network
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=dbachat
    ports:
      - 3308:3308
    volumes:
      - tpvoyageurdb:/var/lib/mysql

networks:
  spring-network:
    name: spring-network

volumes:
  .m2:
  tpvoyageurdb:
